{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run unit tests",
            "type": "shell",
            "command": ". ./run.sh",
            "options": {
                "cwd": "${workspaceFolder}/tests/"
            },
            "group": "test",
            "presentation": {
                "reveal": "always",
                "panel": "dedicated",
                "showReuseMessage": true,
            }
        }
    ]
}